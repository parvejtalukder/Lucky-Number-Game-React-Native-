# Lucky Number Game (React Native)

A simple Android app built with React Native and Expo.  
The app asks the player for a number and generates a random number.  
If the numbers match, the player wins!

## Features
- Background image UI
- Enter a number (1–6)
- Random number generator
- Win / Lose result
